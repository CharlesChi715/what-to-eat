front end :
Take pictures of ingredients. 

send them to my pc. - back end:
Recognize them - agents SDK.
Store the result into inventory (DB in my pc )
inventory get well originzed displayed in web page and shows the expiry date(food that close to expiry date on high priority )
Generate recipes button - input all ingredients to agents SDK. output reciepts

send back to frontend.
Display it in web page.